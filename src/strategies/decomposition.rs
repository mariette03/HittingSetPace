use crate::instance::Instance;
use crate::solve::Status;

/// Solve the instance via a divide (decompose) and conquer approach
fn solve_via_decomposition(instance: &Instance) -> Status {
    //TODO: Use a decomposition approach for solving
    Status::Stop
}
